# ── Model cache (11 GB — too large for GitHub) ──
model_cache/

# ── Checkpoints: keep adapter + training_info, exclude optimizer states ──
checkpoints/checkpoint-*/
checkpoints/checkpoint-*/optimizer.pt
checkpoints/checkpoint-*/rng_state.pth
checkpoints/checkpoint-*/scheduler.pt
checkpoints/checkpoint-*/trainer_state.json
checkpoints/checkpoint-*/training_args.bin

# ── Python ──
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/
*.so

# ── Virtual environments ──
venv/
.venv/
env/

# ── IDE ──
.vscode/
.idea/
*.swp
*~

# ── OS ──
.DS_Store
Thumbs.db
desktop.ini

# ── Temporary / lock files ──
~$*
*.tmp
*.log

# ── HuggingFace cache ──
.cache/